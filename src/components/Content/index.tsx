import React from "react";
import NewBlock from "../Block";

const Content: React.FC<{}> = () => {
  return (
    <React.Fragment>
      <NewBlock></NewBlock>
    </React.Fragment>
  );
};

export default Content;
